# macOS stub — enable when zigbuild darwin targets are added to build-smoke.sh.
GUARDIAN_BIN=target/x86_64-apple-darwin/release/guardian
CURL=curl
CHILD_SHELL="sh -c"
# DYLIB_STAGE=target/x86_64-apple-darwin/release/libfrida-core.dylib
